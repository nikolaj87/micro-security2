server:
  port: 8100
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080
  application:
    name: microservice-job
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka